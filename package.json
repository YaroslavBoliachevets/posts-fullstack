{
	"name": "posts-fullstack",
	"private": true,
	"scripts": {
		"dev": "npm-run-all --parallel dev:frontend dev:backend",
		"dev:frontend": "npm --prefix posts-frontend run dev",
		"dev:backend": "npm --prefix posts-backend run dev"
	},
	"devDependencies": {
		"npm-run-all": "^4.1.5"
	},
	"dependencies": {
		"mobx": "^6.15.0",
		"mobx-react-lite": "^4.1.1"
	}
}
